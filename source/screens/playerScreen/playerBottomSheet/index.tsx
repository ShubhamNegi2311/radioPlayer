import React from 'react';
import {SafeAreaView} from 'react-native';

type PlayerBottomSheetProps = {};

const PlayerBottomSheet: React.FC<PlayerBottomSheetProps> = props => {
  return (
    <SafeAreaView style={{flex: 1, backgroundColor: '#FFFFFF'}}></SafeAreaView>
  );
};

export default PlayerBottomSheet;
