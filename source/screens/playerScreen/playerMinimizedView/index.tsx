import React from 'react';
import {View} from 'react-native';

type PlayerMinimizedViewProps = {};

const PlayerMinimizedView: React.FC<PlayerMinimizedViewProps> = props => {
  return (
    <View style={{backgroundColor: '#FFFFFF'}}>
      <View></View>
    </View>
  );
};

export default PlayerMinimizedView;
