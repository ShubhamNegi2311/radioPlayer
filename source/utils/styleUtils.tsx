/**
 * Some of the common style that are being used through out the app are kept here.
 */

import { StyleSheet } from 'react-native';
import { WHITE } from '../styling/colors';

export const StyleUtils = StyleSheet.create({
  screenContainer: {
    flex: 1,
    backgroundColor: WHITE,
  },
  boldTextStyle: {},
  regularTextStyle: {},
});
